<% @page_title = @category.name %>

<div class="bg-gray-100 py-8">
  <div class="container mx-auto px-4">
    <h1 class="text-3xl font-bold text-gray-900 mb-2"><%= @category.name %></h1>
    <p class="text-gray-600"><%= @category.description %></p>
    <div class="mt-4">
      <%= link_to "← Back to Categories", store_categories_path(@seller.store_slug), class: "text-indigo-600 hover:text-indigo-800" %>
    </div>
  </div>
</div>

<div class="container mx-auto px-4 py-8">
  <% if @products.any? %>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
      <% @products.each do |product| %>
        <div class="bg-white rounded-lg shadow-sm overflow-hidden hover:shadow-md transition-shadow">
          <div class="h-48 bg-gray-200 relative">
            <% if product.cover_image.attached? %>
              <%= image_tag product.cover_image, class: "w-full h-full object-cover" %>
            <% end %>
            <% if product.discounted_price.present? && product.discounted_price < product.price %>
              <div class="absolute top-2 right-2 bg-red-500 text-white text-xs font-bold px-2 py-1 rounded">
                SALE
              </div>
            <% end %>
          </div>
          <div class="p-4">
            <h3 class="text-lg font-medium text-gray-900 truncate"><%= product.name %></h3>
            <div class="mt-2 flex justify-between items-center">
              <div>
                <% if product.discounted_price.present? && product.discounted_price < product.price %>
                  <span class="text-lg font-bold text-gray-900"><%= number_to_currency(product.discounted_price) %></span>
                  <span class="text-sm text-gray-500 line-through ml-2"><%= number_to_currency(product.price) %></span>
                <% else %>
                  <span class="text-lg font-bold text-gray-900"><%= number_to_currency(product.price) %></span>
                <% end %>
              </div>
              <%= link_to "View", product_path(product), class: "text-indigo-600 hover:text-indigo-800" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    
    <div class="mt-8">
      <%= paginate @products %>
    </div>
  <% else %>
    <div class="text-center py-12">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
      </svg>
      <h3 class="mt-4 text-lg font-medium text-gray-900">No products found</h3>
      <p class="mt-1 text-gray-500">This category doesn't have any products yet.</p>
    </div>
  <% end %>
</div>
